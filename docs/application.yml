# ============================================================
# ASPbackup — 標準配置模板 (application.yml)
# ============================================================
# 適用版本: v1.0.0+
# 將此檔案複製為 plugins/ASPbackup/config.yml 即可使用
# ============================================================

# ============================================================
# 1. 備份核心設定
# ============================================================
backup:

  # --- 自動備份